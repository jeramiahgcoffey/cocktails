import { Typography } from '@mui/material'

const Search = () => {
  return (
    <Typography variant='h2' marginBottom={2}>
      {'{Search Component}'}
    </Typography>
  )
}

export default Search
